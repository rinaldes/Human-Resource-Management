<% if params[:action] == "new"%>
  <tr>
<% end %>
  <%= form_for(photo_activity, html: { role: "form", multipart: true}, remote: true) do |f| %>
    <td>
      <div class="checkbox check-default">
        <input id="checkbox12" type="checkbox" value="1">
        <label for="checkbox12"></label>
      </div>
    </td>
    <td>
      <%= f.text_field :name, :class => "full-width", :placeholder => "Photo Activity" %>
    </td>
    <td>
      <div>
        <% if params[:action] == "new" %>
          <%= f.submit "Add", class: "btn btn-success", :onclick => "submit_photo_activity();"%>
        <% else %>
        <%= f.hidden_field :id, :value => params[:id] %>
          <%= f.submit "Update", class: "btn btn-success", :onclick => "update_photo_activity();"%>
        <% end %>
      </div>
    </td>
  <% end %>
<% if params[:action] == "new"%>
  </tr>
<% end %>