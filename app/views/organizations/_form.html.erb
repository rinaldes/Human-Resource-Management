<% if params[:action] == "new"%>
  <tr>
<% end %>
  <%= form_for(organization, html: { role: "form", multipart: true}, remote: true) do |f| %>
    <td>
      <div class="checkbox check-default">
                <input id="checkbox12" type="checkbox" value="1">
                <label for="checkbox12"></label>
            </div>
    </td>
    <% if params[:action] == "new"%>
        <%= f.hidden_field :company_id, :value => params[:company_id_id]%>
      <% else %>
        <%= f.hidden_field :id, :value => params[:id] %>
      <% end %>

    <td>
      <%= f.text_field :name, :class => "full-width", :placeholder => "Name" %>
    </td>
    <td>
      <%= f.text_field :organization_parent, :class => "full-width", :placeholder => "Sub Organization" %>
    </td>
    <td>
    </td>
    <td>
      <div>
        <% if params[:action] == "new" %>
                <%= f.submit "Add", class: "btn btn-success", :onclick => "submit_organization();"%>
              <% else %>
                <%= f.submit "Update", class: "btn btn-success", :onclick => "update_organization();"%>
              <% end %>
            </div>
    </td>
  <% end %>
<% if params[:action] == "new"%>
  </tr>
<% end %>